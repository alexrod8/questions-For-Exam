// How many variables, primitive values, and objects exist in the following code? Identify them explicitly.

let foo = ["launch", "school"];

const bar = foo;

let baz;

const qux = 3;

function quux(foo) {
  return foo * 2;
}

/*
There are 6 variables `foo`, `bar`, `baz` , `qux`, `quux`,and parameter(foo). 
There are 5 primitive values the string `launch`, string `school`, `undefined` , number `3` and number `2`.
Finally there are 2 objects the `function quux` and a array `['launch', 'school']`.

*/